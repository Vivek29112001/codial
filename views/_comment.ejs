<li>
    <p>
        <% if (locals.user && locals.user.id==comment.user._id) { %>
            <small>
                <a href="/comments/destroy/<%= comment.id %>"> Delete</a>
            </small>
            <% } %>
                <%= comment.content %>
                    <br>
                    <small>
                        <%= comment.user.name %>
                    </small>
                    <!-- changes -->
                    <small>
                        <% if(locals.user){ %>
                            <a class="toogle-like-button" data-likes="<%= comment.likes.length%>"
                                href="/likes/toogle/?id=<%=comment._id%>&type=Comment">
                                <%= comment.likes.length %> Likes
                            </a>
                            <% } else { %>
                                <%= comment.likes.length %> Likes
                                    <%} %>
                    </small>       
    </p>
</li>
